import java.util.Comparator;

public class BinarySearcher<T extends Comparable<T>>{

    // For custom comparator
//    public int binarySearchNaive(T array, int length, Comparator<T> comparator) {
//        int leftNode = 0;
//        int rightNode = length - 1;
//    }

    /**
     Binary search implementation in a naive way
     @param elementToBeFound: the element to be matched
     @param dataArray: A non-descending array of arbitrary types
     @param length: the length of the array
     @param option: whether it find the first occurrence, or the last occurrence, or any occurrence
     @return the index of element
     */
    public int binarySearchNaive(T elementToBeFound, T[] dataArray, int length, String option) {

        return 0;
    }
}